class PhoneBook
{
	private:
		int number;
		std::string name;
	public:
};

class Contact
{
	
};

int	main()
{
	string	command;

	while (true)
	{
		std::cin >> command;
		if (strcmp(command, "EXIT")
			return (0);
	}
	return (0);
}
